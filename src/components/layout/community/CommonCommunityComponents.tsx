/**
 *  내용 : 공통 커뮤니티 컴포넌트 (공지사항, 게시판, FAQ 등)
 */

import { Box, styled } from '@mui/material';
import { CenterBox } from '../CommonLayoutComponents';

const CommunityHeader = styled(CenterBox)(({ theme }) => ({
  width: '100%',
}));

const CommunityBody = styled(CenterBox)(({ theme }) => ({
  width: '100%',
}));
